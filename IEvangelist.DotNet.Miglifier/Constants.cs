namespace IEvangelist.DotNet.Miglifier
{
    static class Constants
    {
        internal const string Name = "dotnet miglifier";

        internal const string FullName = "dotnet-miglifier";

        internal const string Description = "Minifies JavaScript files and Uglifies CSS files.";

        internal const string HelpText = @"
You are required to provide the path to the webroot (commonly named 'wwwroot') directory.";

        internal const string PathErrorMessage = "Please specify the path to the webroot (commonly named 'wwwroot') directory.";

        internal const string PathName = "path";

        internal const string PathDescription = "Path to the webroot (commonly named 'wwwroot') directory.";
    }
}